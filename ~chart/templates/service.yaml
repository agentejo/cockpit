apiVersion: v1
kind: Service
metadata:
  name: {{ template "agentejocockpit.fullname" . }}
  labels:
    app: {{ template "agentejocockpit.name" . }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: {{ template "agentejocockpit.name" . }}
  selector:
    app: {{ template "agentejocockpit.name" . }}
    release: {{ .Release.Name }}
